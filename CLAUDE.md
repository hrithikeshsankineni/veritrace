# CLAUDE.md

This project uses **`AGENTS.md`** as the single source of truth for all coding agents.

**Before doing anything, read `AGENTS.md` in full, then read `PROGRESS.md` for the current state.** Also read `Veritrace_System_Design.md` and `Veritrace_Problem_Data_Evaluation.md` once for product context.

Follow `AGENTS.md` exactly — the repo structure, the ordered tasks in `PROGRESS.md`, the coding standards, and especially the **collaboration protocol** (§8): mark a task `IN PROGRESS` before starting, finish at clean boundaries with passing tests, commit, and tick it `DONE`. You share this repo with another agent (Codex); never edit a task another agent has locked.

Do not duplicate the brief here — `AGENTS.md` is authoritative.
